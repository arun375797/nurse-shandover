# Leave empty so the browser calls same-origin `/api`.
# Locally Vite proxies to the backend; on Vercel, vercel.json rewrites `/api` → Render.
# Do NOT set this to the Render URL in production — Safari/iOS blocks those cross-site cookies.
VITE_API_URL=
