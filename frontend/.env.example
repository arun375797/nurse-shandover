# Leave empty for local dev (Vite proxies /api → localhost:4000).
# On Vercel, set to your Render API origin (no trailing slash, no /api):
# VITE_API_URL=https://nurse-shandover.onrender.com
VITE_API_URL=
