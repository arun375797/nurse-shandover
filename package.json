{
  "name": "bedsiderelay",
  "version": "1.0.0",
  "private": true,
  "description": "BedsideRelay - Clinical Shift Handover (separate frontend + backend)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c teal,blue \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "lint": "eslint frontend/src backend/src --max-warnings=100",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript-eslint": "^8.26.1"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module"
}
